# Eoynx (이오닉스) - AI Agent Gateway Platform
> "어둠을 가르고 시작되는 새벽" - AI와 웹의 새로운 전환점을 열다

## 📍 서비스 개요
Eoynx는 AI 에이전트를 위한 전용 인터페이스를 제공합니다.
HTML 크롤링 없이 JSON API를 통해 모든 기능에 접근할 수 있습니다.

## 🔗 핵심 엔드포인트

### 취급 설명서 (필독!)
- `GET /api/ai-manifest.json` - 전체 API 명세 및 사용 가이드

### 정보 조회
- `GET /api/agent` - 사이트 컨텍스트 및 가용 액션 목록
- `GET /api/agent/search?q={keyword}` - 구조화된 검색
- `GET /api/agent/health` - 서비스 상태 확인

### 인증
- `POST /api/agent/auth/token` - 에이전트 토큰 발급

### 액션 실행
- `POST /api/agent/action` - 명령어 기반 액션 실행
- `POST /api/agent/sandbox` - 샌드박스 시뮬레이션 (안전 테스트)

## 🎯 사용 가능한 액션
| 액션 ID | 설명 | 권한 | 확인 필요 |
|---------|------|------|----------|
| search | 상품/콘텐츠 검색 | read | ❌ |
| add_to_cart | 장바구니 추가 | write | ❌ |
| view_cart | 장바구니 조회 | read | ❌ |
| purchase | 구매 진행 | execute | ✅ |
| subscribe_alert | 알림 구독 | write | ❌ |

## 🔐 인증 방법
모든 write/execute 액션은 인증이 필요합니다.

```
Authorization: Bearer <your-token>
또는
X-Agent-Token: <your-token>
```

토큰 발급:
```json
POST /api/agent/auth/token
{
  "agentId": "your-agent-id",
  "agentSecret": "your-secret-key"
}
```

## ⚠️ 주의사항
1. Rate Limit: 분당 100회, 일일 10,000회
2. execute 권한 액션(구매 등)은 반드시 `confirmed: true` 필요
3. HTML 파싱 금지 - 항상 JSON API 사용
4. 무한 루프 요청 금지

## 📊 응답 형식
모든 응답은 다음 형식을 따릅니다:
```json
{
  "success": true,
  "data": { ... },
  "meta": {
    "timestamp": "2026-02-03T10:00:00Z",
    "requestId": "uuid",
    "version": "1.0.0"
  }
}
```

## 🆘 문의
- 기술 지원: support@agent-gateway.dev
- 문서: https://docs.agent-gateway.dev

---
Last Updated: 2026-02-03
Version: 1.0.0
